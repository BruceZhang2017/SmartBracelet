# WatchProtocolSDK 接入说明文档

## 📋 目录
1. [SDK 简介](#sdk-简介)
2. [系统要求](#系统要求)
3. [集成步骤](#集成步骤)
4. [初始化配置](#初始化配置)
5. [核心功能](#核心功能)
6. [API 参考](#api-参考)
7. [示例代码](#示例代码)
8. [常见问题](#常见问题)
9. [版本历史](#版本历史)

---

## SDK 简介

WatchProtocolSDK 是一个用于智能手表设备通信的 iOS 框架，提供了完整的蓝牙连接、数据同步和设备管理功能。

### 主要特性

- ✅ **蓝牙连接管理**：自动扫描、连接和重连智能手表设备
- ✅ **健康数据同步**：步数、睡眠、心率、血氧、血压数据同步
- ✅ **设备控制**：闹钟、久坐提醒、喝水提醒等功能设置
- ✅ **消息推送**：支持来电、短信、第三方应用消息推送
- ✅ **OTA 升级**：固件空中升级功能
- ✅ **天气同步**：实时天气数据推送到手表
- ✅ **联系人同步**：手机联系人同步到手表
- ✅ **无依赖**：零外部依赖，仅依赖系统框架

---

## 系统要求

| 项目 | 要求 |
|------|------|
| iOS 版本 | iOS 12.0 及以上 |
| Xcode | Xcode 13.0 及以上 |
| Swift | Swift 5.0 及以上 |
| 架构支持 | arm64（真机）、arm64 + x86_64（模拟器） |
| 权限要求 | 蓝牙权限（NSBluetoothAlwaysUsageDescription） |

---

## 集成步骤

### 方式一：XCFramework 集成（推荐）

#### 1. 选择合适的版本

```
WatchProtocolSDK-Distribution/
├── Debug/
│   └── WatchProtocolSDK.xcframework    # 调试版本，包含调试符号
└── Release/
    └── WatchProtocolSDK.xcframework    # 发布版本，体积更小、性能更优
```

**建议**：
- 开发阶段使用 `Debug` 版本
- 提交 App Store 使用 `Release` 版本

#### 2. 添加到项目

1. 将 `WatchProtocolSDK.xcframework` 拖入 Xcode 项目
2. 在 **General** → **Frameworks, Libraries, and Embedded Content** 中确认框架已添加
3. 设置为 **Embed & Sign**（如果使用动态框架）

#### 3. 配置 Info.plist

添加蓝牙权限描述：

```xml
<key>NSBluetoothAlwaysUsageDescription</key>
<string>需要使用蓝牙连接智能手表设备</string>
<key>NSBluetoothPeripheralUsageDescription</key>
<string>需要使用蓝牙与手表进行数据传输</string>
```

#### 4. 导入框架

在需要使用 SDK 的文件中导入：

```swift
import WatchProtocolSDK
```

---

## 初始化配置

### 1. 设置数据存储代理（必需）

SDK 采用委托模式进行数据存储，需要实现 `WatchDataStorageDelegate` 协议：

```swift
import WatchProtocolSDK

class DatabaseManager: WatchDataStorageDelegate {

    static let shared = DatabaseManager()

    // 保存步数数据
    func saveStepData(_ data: WatchProtocolSDK.StepData) {
        // 实现你的数据库保存逻辑
        print("保存步数: \(data.step) 步，日期: \(data.date)")
    }

    // 保存睡眠数据
    func saveSleepData(_ data: WatchProtocolSDK.SleepData) {
        print("保存睡眠: 深睡 \(data.deep)分钟，浅睡 \(data.light)分钟")
    }

    // 保存心率数据
    func saveHeartRateData(_ data: WatchProtocolSDK.HeartRateData) {
        print("保存心率: \(data.heartRate) bpm")
    }

    // 保存血氧数据
    func saveOxygenData(_ data: WatchProtocolSDK.OxygenData) {
        print("保存血氧: \(data.oxygen)%")
    }

    // 保存血压数据
    func saveBloodPressureData(_ data: WatchProtocolSDK.BloodPressureData) {
        print("保存血压: \(data.systolic)/\(data.diastolic) mmHg")
    }
}
```

### 2. 在 AppDelegate 中初始化

```swift
import UIKit
import WatchProtocolSDK

@UIApplicationMain
class AppDelegate: UIResponder, UIApplicationDelegate {

    func application(_ application: UIApplication,
                     didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {

        // 设置 SDK 数据存储委托
        XGZTBlueToothManager.shared.storageDelegate = DatabaseManager.shared

        return true
    }
}
```

---

## 核心功能

### 1. 设备扫描与连接

```swift
import WatchProtocolSDK

// 开始扫描设备
XGZTBlueToothManager.shared.startScan()

// 设置蓝牙管理器代理
XGZTBlueToothManager.shared.delegate = self

// 实现代理方法
extension ViewController: BleManagerDelegate {

    // 发现设备
    func didDiscoverDevice(device: BluetoothWatchDevice, rssi: NSNumber) {
        print("发现设备: \(device.deviceName ?? "Unknown"), RSSI: \(rssi)")
    }

    // 连接成功
    func didConnectDevice() {
        print("设备连接成功")
        // 读取设备信息
        XGZTBlueToothManager.shared.handler.readDeviceInfo()
    }

    // 连接失败
    func didFailToConnect(error: Error?) {
        print("连接失败: \(error?.localizedDescription ?? "")")
    }

    // 设备断开
    func didDisconnectDevice() {
        print("设备已断开")
    }
}

// 连接指定设备
XGZTBlueToothManager.shared.connectAndScan(to: macAddress, deviceName: deviceName)

// 断开连接
XGZTBlueToothManager.shared.disconnect()
```

### 2. 读取设备信息

```swift
// 读取设备基本信息
XGZTBlueToothManager.shared.handler.readDeviceInfo()

// 获取当前连接的设备
if let device = XGZTBlueToothManager.shared.device {
    print("设备名称: \(device.deviceName ?? "")")
    print("设备型号: \(device.deviceModel ?? 0)")
    print("电池电量: \(device.batteryLevel ?? 0)%")
    print("固件版本: \(device.firmwareVersion ?? "")")
    print("屏幕类型: \(device.screenType == 1 ? "方形" : "圆形")")
    print("屏幕尺寸: \(device.screenWidth) x \(device.screenHeight)")
}
```

### 3. 同步健康数据

```swift
// 同步步数数据
XGZTBlueToothManager.shared.handler.syncStepData()

// 同步睡眠数据
XGZTBlueToothManager.shared.handler.syncSleepData()

// 同步心率数据
XGZTBlueToothManager.shared.handler.syncHeartRateData()

// 同步血氧数据
XGZTBlueToothManager.shared.handler.syncOxygenData()

// 同步血压数据
XGZTBlueToothManager.shared.handler.syncBloodPressureData()

// 读取实时健康数据
if let device = XGZTBlueToothManager.shared.device {
    print("当前步数: \(device.currentStep)")
    print("当前心率: \(device.currentHeartrate) bpm")
    print("当前血氧: \(device.currentOxygen)%")
}
```

### 4. 闹钟管理

```swift
// 创建闹钟
let alarm = AlarmData(
    alarmIndex: 0,          // 闹钟索引 (0-9)
    mswitch: 1,             // 开关 (0:关闭, 1:开启)
    alarmCycle: 127,        // 重复周期 (位掩码: 周日-周六)
    alarmHour: 7,           // 小时 (0-23)
    alarmMinute: 30,        // 分钟 (0-59)
    vibrationMode: 1,       // 震动模式
    remindLater: 5          // 稍后提醒时间(分钟)
)

// 设置闹钟
XGZTBlueToothManager.shared.handler.setAlarm(alarm)

// 读取闹钟列表
XGZTBlueToothManager.shared.handler.readAlarms()

// 获取闹钟数据
if let device = XGZTBlueToothManager.shared.device {
    print("闹钟总数: \(device.alarmcount)")
    for alarm in device.alarms {
        print("闹钟\(alarm.alarmIndex): \(alarm.alarmHour):\(alarm.alarmMinute)")
    }
}
```

### 5. 久坐/喝水提醒

```swift
// 设置久坐提醒
let longsitReminder = ReminderInfoResponse(
    eventType: 1,       // 事件类型: 1=久坐提醒
    cycle: 127,         // 重复周期 (位掩码)
    startHour: 9,       // 开始小时
    startMinute: 0,     // 开始分钟
    endHour: 18,        // 结束小时
    endMinute: 0,       // 结束分钟
    period: 60          // 提醒间隔(分钟)
)
XGZTBlueToothManager.shared.handler.setLongsitReminder(longsitReminder)

// 设置喝水提醒
let drinkReminder = ReminderInfoResponse(
    eventType: 2,       // 事件类型: 2=喝水提醒
    cycle: 127,
    startHour: 8,
    startMinute: 0,
    endHour: 22,
    endMinute: 0,
    period: 120
)
XGZTBlueToothManager.shared.handler.setDrinkWaterReminder(drinkReminder)
```

### 6. 消息推送

```swift
// 推送来电通知
XGZTBlueToothManager.shared.handler.pushIncomingCall(name: "张三", phoneNumber: "13800138000")

// 推送短信
XGZTBlueToothManager.shared.handler.pushMessage(title: "短信", content: "您有一条新消息")

// 推送微信消息
XGZTBlueToothManager.shared.handler.pushWeChatMessage(sender: "李四", content: "你好")

// 推送 QQ 消息
XGZTBlueToothManager.shared.handler.pushQQMessage(sender: "王五", content: "在吗")

// 推送其他应用消息
XGZTBlueToothManager.shared.handler.pushAppNotification(
    appType: .facebook,  // 应用类型
    title: "Facebook",
    content: "You have a new notification"
)
```

### 7. 设备设置

```swift
// 设置设备参数
XGZTBlueToothManager.shared.handler.setUserInfo(
    sex: 1,         // 性别 (0:男, 1:女)
    age: 30,        // 年龄
    height: 175,    // 身高(cm)
    weight: 70      // 体重(kg)
)

// 设置时间格式
XGZTBlueToothManager.shared.handler.setTimeFormat(is24Hour: true)

// 设置单位制
XGZTBlueToothManager.shared.handler.setUnitSystem(isMetric: true)

// 设置抬手亮屏
XGZTBlueToothManager.shared.handler.setRaiseToWake(enabled: true)

// 查找手机
XGZTBlueToothManager.shared.handler.findPhone()
```

### 8. 天气同步

```swift
// 推送天气数据
let weatherData = WeatherData(
    temperature: 25,        // 温度(℃)
    weatherType: 1,         // 天气类型 (0:晴, 1:多云, 2:雨...)
    minTemperature: 18,     // 最低温度
    maxTemperature: 28,     // 最高温度
    airQuality: 50          // 空气质量指数
)
XGZTBlueToothManager.shared.handler.syncWeather(weatherData)
```

---

## API 参考

### 核心类

#### XGZTBlueToothManager

蓝牙管理器，单例模式。

```swift
public class XGZTBlueToothManager {
    public static let shared: XGZTBlueToothManager

    // 属性
    public weak var delegate: BleManagerDelegate?
    public weak var storageDelegate: WatchDataStorageDelegate?
    public var device: BluetoothWatchDevice?
    public var handler: XGZTBusinessHandler

    // 方法
    public func startScan()
    public func stopScan()
    public func connectAndScan(to macAddress: String, deviceName: String)
    public func disconnect()
}
```

#### BluetoothWatchDevice

设备模型类。

```swift
public class BluetoothWatchDevice {
    // 设备信息
    public var deviceName: String?
    public var max: String?              // MAC 地址
    public var batteryLevel: Int?
    public var isCharging: Bool?
    public var firmwareVersion: String?
    public var hardwareVersion: Int?
    public var screenType: Int           // 1:方形, 2:圆形
    public var screenWidth: Int
    public var screenHeight: Int
    public var mtu: Int

    // 用户信息
    public var sex: Int
    public var age: Int
    public var height: Int
    public var weight: Int

    // 健康数据
    public var currentStep: Int
    public var currentHeartrate: Int
    public var currentOxygen: Int
    public var currentSystolicpressure: Int
    public var currentDiastolicpressure: Int

    // 闹钟和提醒
    public var alarms: [AlarmData]
    public var longsit: ReminderInfoResponse?
    public var drinkWater: ReminderInfoResponse?

    // 静态方法
    public static func loadFromSandbox(mac: String) -> BluetoothWatchDevice?
    public static func loadFromSandbox(deviceName: String) -> BluetoothWatchDevice?
    public static func loadAll()
    public static func deleteFromSandbox(mac: String)
}
```

#### WatchDataStorageDelegate

数据存储代理协议。

```swift
public protocol WatchDataStorageDelegate: AnyObject {
    func saveStepData(_ data: StepData)
    func saveSleepData(_ data: SleepData)
    func saveHeartRateData(_ data: HeartRateData)
    func saveOxygenData(_ data: OxygenData)
    func saveBloodPressureData(_ data: BloodPressureData)
}
```

### 数据结构

#### StepData - 步数数据

```swift
public struct StepData {
    public let date: String      // 日期 (格式: yyyy-MM-dd)
    public let mac: String       // 设备 MAC 地址
    public let step: Int         // 步数
}
```

#### SleepData - 睡眠数据

```swift
public struct SleepData {
    public let date: String      // 日期
    public let mac: String       // 设备 MAC 地址
    public let awake: Int        // 清醒时长(分钟)
    public let light: Int        // 浅睡时长(分钟)
    public let deep: Int         // 深睡时长(分钟)
}
```

#### HeartRateData - 心率数据

```swift
public struct HeartRateData {
    public let time: Int         // 时间戳
    public let mac: String       // 设备 MAC 地址
    public let heartRate: Int    // 心率值(bpm)
}
```

#### AlarmData - 闹钟数据

```swift
public struct AlarmData {
    public var alarmIndex: Int      // 闹钟索引 (0-9)
    public var mswitch: Int         // 开关 (0:关, 1:开)
    public var alarmCycle: Int      // 重复周期 (位掩码)
    public var alarmHour: Int       // 小时 (0-23)
    public var alarmMinute: Int     // 分钟 (0-59)
    public var vibrationMode: Int   // 震动模式
    public var remindLater: Int     // 稍后提醒(分钟)
}
```

---

## 示例代码

### 完整的设备管理示例

```swift
import UIKit
import WatchProtocolSDK

class DeviceViewController: UIViewController {

    // MARK: - Properties

    private var discoveredDevices: [BluetoothWatchDevice] = []

    // MARK: - Lifecycle

    override func viewDidLoad() {
        super.viewDidLoad()
        setupBluetooth()
    }

    // MARK: - Setup

    private func setupBluetooth() {
        // 设置代理
        XGZTBlueToothManager.shared.delegate = self

        // 开始扫描
        XGZTBlueToothManager.shared.startScan()
    }

    // MARK: - Actions

    func connectDevice(device: BluetoothWatchDevice) {
        guard let mac = device.max,
              let name = device.deviceName else { return }

        XGZTBlueToothManager.shared.connectAndScan(to: mac, deviceName: name)
    }

    func syncAllData() {
        let handler = XGZTBlueToothManager.shared.handler

        // 同步所有健康数据
        handler.syncStepData()
        handler.syncSleepData()
        handler.syncHeartRateData()
        handler.syncOxygenData()
        handler.syncBloodPressureData()
    }
}

// MARK: - BleManagerDelegate

extension DeviceViewController: BleManagerDelegate {

    func didDiscoverDevice(device: BluetoothWatchDevice, rssi: NSNumber) {
        // 添加到设备列表
        if !discoveredDevices.contains(where: { $0.max == device.max }) {
            discoveredDevices.append(device)
            // 更新 UI
        }
    }

    func didConnectDevice() {
        print("✅ 设备连接成功")

        // 读取设备信息
        XGZTBlueToothManager.shared.handler.readDeviceInfo()

        // 等待设备信息读取完成后同步数据
        DispatchQueue.main.asyncAfter(deadline: .now() + 2) {
            self.syncAllData()
        }
    }

    func didFailToConnect(error: Error?) {
        print("❌ 连接失败: \(error?.localizedDescription ?? "未知错误")")
    }

    func didDisconnectDevice() {
        print("⚠️ 设备已断开")
    }

    func didReceiveData(data: Data) {
        // 接收到原始数据
    }
}
```

---

## 常见问题

### Q1: 如何判断设备是否已连接？

```swift
let isConnected = XGZTBlueToothManager.shared.isConnected
// 或
let connectionState = XGZTConnectionStateManager.shared.connectionState
```

### Q2: 如何处理重连逻辑？

SDK 会自动处理重连，但你也可以手动触发：

```swift
// 在设备断开时
func didDisconnectDevice() {
    // 获取上次连接的设备
    if let lastDevice = BluetoothWatchDevice.loadFromSandbox(mac: lastMacAddress) {
        // 尝试重新连接
        DispatchQueue.main.asyncAfter(deadline: .now() + 2) {
            XGZTBlueToothManager.shared.connectAndScan(
                to: lastDevice.max!,
                deviceName: lastDevice.deviceName!
            )
        }
    }
}
```

### Q3: 数据同步失败怎么办？

1. 检查设备是否已连接
2. 确认蓝牙权限已授予
3. 检查设备电量是否充足
4. 尝试重新连接设备

```swift
// 检查连接状态
if !XGZTBlueToothManager.shared.isConnected {
    print("设备未连接，无法同步数据")
    return
}

// 重试机制
func syncDataWithRetry(maxRetries: Int = 3) {
    var retryCount = 0

    func attemptSync() {
        XGZTBlueToothManager.shared.handler.syncStepData()

        // 如果失败，重试
        DispatchQueue.main.asyncAfter(deadline: .now() + 5) {
            if retryCount < maxRetries && !self.syncSuccess {
                retryCount += 1
                attemptSync()
            }
        }
    }

    attemptSync()
}
```

### Q4: 如何支持多设备切换？

```swift
// 保存设备列表
BluetoothWatchDevice.saveToSandbox(device: device)

// 加载所有已保存的设备
BluetoothWatchDevice.loadAll()

// 切换到新设备
func switchDevice(to newDevice: BluetoothWatchDevice) {
    // 断开当前设备
    XGZTBlueToothManager.shared.disconnect()

    // 连接新设备
    DispatchQueue.main.asyncAfter(deadline: .now() + 1) {
        XGZTBlueToothManager.shared.connectAndScan(
            to: newDevice.max!,
            deviceName: newDevice.deviceName!
        )
    }
}

// 删除设备
BluetoothWatchDevice.deleteFromSandbox(mac: macAddress)
```

### Q5: 如何优化蓝牙连接稳定性？

```swift
// 1. 设置合理的扫描超时
XGZTBlueToothManager.shared.scanTimeout = 15.0

// 2. 监听应用前后台切换
NotificationCenter.default.addObserver(
    self,
    selector: #selector(appDidBecomeActive),
    name: UIApplication.didBecomeActiveNotification,
    object: nil
)

@objc func appDidBecomeActive() {
    // 应用回到前台时检查连接
    if !XGZTBlueToothManager.shared.isConnected {
        // 尝试重连
        reconnectLastDevice()
    }
}

// 3. 实现心跳检测
Timer.scheduledTimer(withTimeInterval: 30, repeats: true) { _ in
    if XGZTBlueToothManager.shared.isConnected {
        // 发送心跳包
        XGZTBlueToothManager.shared.handler.readBatteryLevel()
    }
}
```

---

## 版本历史

### v1.0.0 (2024-12-28)

**新特性**
- ✅ 首次发布
- ✅ 完整的蓝牙设备连接和管理功能
- ✅ 健康数据同步（步数、睡眠、心率、血氧、血压）
- ✅ 闹钟和提醒功能
- ✅ 消息推送支持
- ✅ 委托模式数据存储
- ✅ 零外部依赖

**架构改进**
- ✅ 移除 RealmSwift 依赖，采用委托模式
- ✅ 提供 Debug 和 Release 两个版本
- ✅ 支持 iOS 真机和模拟器（XCFramework）

---

## 技术支持

如有问题或建议，请联系技术支持团队。

**开发文档**: 参见 SDK 源码注释
**更新日期**: 2024-12-28
**SDK 版本**: 1.0.0

---

© 2024 WatchProtocolSDK. All rights reserved.
