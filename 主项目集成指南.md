# 主项目集成指南 - WatchProtocolSDK

本指南说明如何在 SmartBracelet 主项目中使用 WatchProtocolSDK Framework。

---

## 📦 Framework 已就绪

WatchProtocolSDK Framework 已成功创建并验证:
- ✅ 编译成功 (BUILD SUCCEEDED)
- ✅ 1000+ 公开符号正确导出
- ✅ 所有核心 API 可用

Framework 位置:
```
~/Library/Developer/Xcode/DerivedData/SmartBracelet-*/Build/Products/Debug-iphoneos/WatchProtocolSDK.framework
```

---

## 🔧 当前项目状态

### 代码结构已更新

原有的 `huaxin/WatchProtocol` 文件已迁移到 Framework:

**旧路径** → **新路径**:
```
SmartBracelet/huaxin/XGZTBlueToothManager.swift
  → WatchProtocolSDK/Core/XGZTBlueToothManager.swift

SmartBracelet/huaxin/XGZTBusinessHandler.swift
  → WatchProtocolSDK/Core/XGZTBusinessHandler.swift

SmartBracelet/huaxin/XGZTCommands.swift
  → WatchProtocolSDK/Core/XGZTCommands.swift

SmartBracelet/huaxin/DatabaseManager.swift
  → WatchProtocolSDK/Models/DatabaseManager.swift

SmartBracelet/huaxin/XGZTSwitchDevice.swift
  → WatchProtocolSDK/Models/XGZTSwitchDevice.swift
```

### 项目配置说明

SmartBracelet.xcworkspace 已配置:
- ✅ 主项目 Target (SmartBracelet)
- ✅ Framework Target (WatchProtocolSDK)
- ✅ Pods.xcodeproj (CocoaPods 依赖)

---

## 🚀 使用 Framework 的两种方式

### 方式1: 直接使用 (当前方式 - 推荐)

Framework 和主项目在同一个 workspace 中,可以直接导入使用:

#### 步骤1: 在需要使用的文件中导入
```swift
import WatchProtocolSDK
```

#### 步骤2: 使用 Framework API
```swift
// 原有代码保持不变,只需添加 import
class MTabBarController: UITabBarController {
    override func viewDidLoad() {
        super.viewDidLoad()

        // Framework API 可以直接使用
        XGZTBlueToothManager.shared.initCentral()

        // 原有的设备管理代码保持不变
        XGZTDeviceManager.shared.loadDevicesFromDatabase()
    }
}
```

#### 步骤3: 确保 Target 依赖关系

在 Xcode 中:
1. 选择 **SmartBracelet** Target
2. 进入 **General** → **Frameworks, Libraries, and Embedded Content**
3. 确认 `WatchProtocolSDK.framework` 已添加且设置为 **Do Not Embed**
   - 因为在同一 workspace,选择 Do Not Embed 即可
   - 如果分发给其他项目,则需要 Embed & Sign

---

### 方式2: 独立 Framework (分发给其他团队)

如果要将 Framework 分发给其他团队的项目:

#### 步骤1: 导出 Framework

找到编译好的 Framework:
```bash
~/Library/Developer/Xcode/DerivedData/SmartBracelet-*/Build/Products/Debug-iphoneos/WatchProtocolSDK.framework
```

#### 步骤2: 在其他项目中集成

1. 将 `WatchProtocolSDK.framework` 拖入目标项目
2. 在 Target Settings → **Frameworks, Libraries, and Embedded Content** 中:
   - 添加 Framework
   - 设置为 **Embed & Sign**
3. 确保目标项目也链接了 RealmSwift

#### 步骤3: 配置 RealmSwift 依赖

在目标项目的 Podfile 中:
```ruby
target 'OtherProject' do
  pod 'RealmSwift', '~> 10.0'
end
```

然后运行:
```bash
pod install
```

---

## 📋 主项目中需要修改的代码

### 需要添加 import 的文件

所有使用蓝牙功能的文件都需要添加:
```swift
import WatchProtocolSDK
```

**关键文件列表**:
1. **MTabBarController.swift** - 主界面,初始化蓝牙管理器
2. **DeviceViewController.swift** - 设备列表,扫描和连接
3. **BindDeviceController.swift** - 绑定设备
4. **所有使用 XGZTBlueToothManager 的文件**
5. **所有使用 XGZTBusinessHandler 的文件**
6. **所有使用 BluetoothWatchDevice 的文件**

### 代码迁移示例

**迁移前**:
```swift
class DeviceViewController: UIViewController {
    func scanDevices() {
        // 直接使用,无需 import
        XGZTBlueToothManager.shared.startScanning()
    }
}
```

**迁移后**:
```swift
import WatchProtocolSDK  // ← 添加这一行

class DeviceViewController: UIViewController {
    func scanDevices() {
        // 代码保持不变
        XGZTBlueToothManager.shared.startScanning()
    }
}
```

---

## ✅ 验证集成

### 快速验证步骤

1. **编译主项目**:
   ```bash
   xcodebuild -workspace SmartBracelet.xcworkspace \
              -scheme SmartBracelet \
              -sdk iphoneos \
              clean build
   ```

2. **检查 import 是否成功**:

   在任一 ViewController 中添加:
   ```swift
   import WatchProtocolSDK

   func testFramework() {
       // 测试核心管理类
       let bleManager = XGZTBlueToothManager.shared
       print("✅ Framework 导入成功: \(type(of: bleManager))")

       // 测试设备管理
       let deviceManager = XGZTDeviceManager.shared
       print("✅ 设备缓存数: \(deviceManager.deviceCount)")

       // 测试数据模型
       let device = BluetoothWatchDevice()
       device.deviceName = "测试设备"
       print("✅ 数据模型可用: \(device.deviceName ?? "")")
   }
   ```

3. **运行测试**:

   在 AppDelegate 的 `didFinishLaunchingWithOptions` 中调用:
   ```swift
   #if DEBUG
   testFramework()
   #endif
   ```

---

## 🔧 可能遇到的问题

### 问题1: "No such module 'WatchProtocolSDK'"

**原因**: Framework 未正确链接到主项目

**解决方案**:
1. 在 Xcode 中选择 SmartBracelet Target
2. Build Phases → **Dependencies** → 添加 WatchProtocolSDK
3. Clean Build Folder (Cmd+Shift+K)
4. 重新编译

### 问题2: "Cannot find 'XGZTBlueToothManager' in scope"

**原因**: 忘记添加 `import WatchProtocolSDK`

**解决方案**:
在文件顶部添加:
```swift
import WatchProtocolSDK
```

### 问题3: RealmSwift 冲突

**原因**: 主项目和 Framework 都链接了不同版本的 RealmSwift

**解决方案**:
确保主项目和 Framework 使用相同版本的 RealmSwift:
```ruby
# Podfile
pod 'RealmSwift', '~> 10.0'
```

---

## 📊 迁移检查清单

在完成集成后,检查以下项目:

- [ ] WatchProtocolSDK Framework 编译成功
- [ ] 主项目 Target 依赖 WatchProtocolSDK
- [ ] 所有使用蓝牙功能的文件添加了 `import WatchProtocolSDK`
- [ ] MTabBarController 中初始化代码正常工作
- [ ] 设备扫描功能正常
- [ ] 设备连接功能正常
- [ ] 数据同步功能正常
- [ ] Realm 数据库访问正常
- [ ] 测试编译通过
- [ ] 真机运行无崩溃

---

## 🎯 下一步建议

### 立即可做:

1. **验证 Framework 导入**
   - 在关键文件中添加 `import WatchProtocolSDK`
   - 编译主项目确认无错误

2. **运行基本测试**
   - 测试设备扫描
   - 测试设备连接
   - 测试数据同步

3. **真机测试**
   - 在真实 iOS 设备上测试蓝牙功能
   - 验证设备发现和连接
   - 验证数据同步

### 可选优化:

1. **创建简化 API 包装器**
   - 封装更简单的公开接口
   - 隐藏 Framework 内部实现细节

2. **添加错误处理**
   - 统一的错误处理机制
   - 用户友好的错误提示

3. **性能优化**
   - 监控内存使用
   - 优化数据库查询

---

## 📚 相关文档

- **WatchProtocolSDK-最终报告.md** - Framework 创建完整报告
- **WatchProtocolSDK-验证报告.md** - Framework 验证和符号导出报告
- **WatchProtocolSDK-集成测试.swift** - 集成测试代码和使用示例
- **WatchProtocolSDK-设计文档.md** - 架构设计和技术方案

---

**文档创建者**: Claude Sonnet 4.5
**创建日期**: 2025-12-27
**适用于**: SmartBracelet 主项目 + WatchProtocolSDK Framework

✅ **集成指南完成,可以开始使用 Framework!**
